/**
 * The immutable data-document tree model.
 *
 * Published as `@ltr8/tson/tree` so a consumer that only walks documents never pulls the schema
 * compiler into their bundle.
 */
export * from './nodes.js';
